# Image URI

Công cụ lấy ảnh từ trong máy tính thông qua đường dẫn tới tệp ảnh.

**Ứng dụng phổ biến:**
- :white_check_mark: Là đầu vào cho tất cả các công cụ xử lý ảnh.

## Video hướng dẫn
<VideoPlayer url='/videos/imagesource_uri/imagesource_uri.mp4' youtubeUrl='https://www.youtube.com/watch?v=bXbPIFDfoFA' />

## Định dạng hỗ trợ

Danh sách định dạng ảnh hỗ trợ: `*.bmp, *.dib, *.png, *.jpeg, *.jpg, *.jp2, *.webp, *.pbm, *.pgm, *.ppm *.pxm, *.pnm, *.sr, *.ras, *.tiff, *.tif, *.exr, *.hdr, *.pic`

Ảnh màu hỗ trợ: `ảnh gray, ảnh 3 channel RGB, ảnh 4 channel RGBA`.

## Đường dẫn Ảnh
Đường dẫn ảnh có thể nhập tay thủ công hoặc lựa chọn từ những công cụ đằng trước đó.