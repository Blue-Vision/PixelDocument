# Value Comparison

Công cụ so sánh dữ liệu và cho ra kết luận True/False.

**Ứng dụng phổ biến:**
- :white_check_mark: So sánh dữ liệu, điều hướng logic.

[Tham khảo công cụ If Else Logic](/docs/modules/if_else_logic) 

[Tham khảo công cụ Combine Logic](/docs/modules/combine_logic) 

## Video hướng dẫn
<VideoPlayer url='/videos/value_comparison/value_comparison.mp4' youtubeUrl='https://www.youtube.com/watch?v=4OSVrFmH0a8' />
Các công cụ hỗ trợ được sử dụng trong video:
-   [Nguồn ảnh - Imagesource](/docs/modules/Imagesource)
-   [Đếm pixel - Count Pixel Color](/docs/modules/count_pixel_color)

## Cài đặt

Các dạng so sánh hỗ trợ:
-   **True/False**: dữ liệu so sánh dạng true/false, lựa chọn dữ liệu này từ các module đằng trước. Hỗ trợ kiểu so sánh:
    -   **Equal**: 2 dữ liệu giống nhau sẽ được kết luận là đúng (True).
    -   **Not Equal**: 2 dữ liệu này trái ngược nhau sẽ được kết luận là đúng (True).
-   **Dạng số**: dữ liệu so sánh đầu vào dạng số, hỗ trợ các kiểu so sánh só học: `=` `!=` `<` `<=` `>` `>=`.
-   **Dạng chữ**: dữ liệu so sánh dạng chữ, hỗ trợ kiểu so sánh:
    -   **Equal**: 2 dữ liệu giống nhau sẽ được kết luận là đúng (True).
    -   **Not Equal**: 2 dữ liệu này trái ngược nhau sẽ được kết luận là đúng (True).
    -   **Contain**: nếu dữ liệu đầu tiên chứa dữ liệu thứ 2 sẽ được kết luận là đúng (True).
    -   **Not Contain**: nếu dữ liệu đầu tiên **không** chứa dữ liệu thứ 2 sẽ được kết luận là đúng (True).